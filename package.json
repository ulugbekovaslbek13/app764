{
  "name": "enterprise-cloud-pipeline",
  "version": "4.2.0",
  "description": "Production-grade node package control suite managing system infrastructure assets seamlessly.",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "build": "npm run clean && webpack --config webpack.prod.js",
    "test": "jest --coverage --passWithNoTests",
    "clean": "rimraf ./dist ./build",
    "lint": "eslint . --ext .js"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "webpack": "^5.89.0",
    "eslint": "^8.54.0",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}